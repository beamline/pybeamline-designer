{
  "type": "object",
  "properties": {
    "id": {
      "$ref": "definitions.json#/definitions/id"
    },
    "descriptors": {
      "type": "object",
      "properties": {
        "name": {
          "const": "skip_events"
        },
        "outputType": {
          "oneOf": [
            {
              "const": null
            },
            {
              "$ref": "definitions.json#/definitions/streamTypes/any"
            }
          ]
        },
        "inputType": {
          "oneOf": [
            {
              "const": null
            },
            {
              "$ref": "definitions.json#/definitions/streamTypes/any"
            }
          ]
        }
      }
    },
    "parameters": {
      "type": "object",
      "properties": {
        "events_to_skip": {
          "type": "string",
          "pattern": "^[0-9]+$"
        }
      },
      "required": [
        "events_to_skip"
      ]
    },
    "outputs": {
      "$ref": "definitions.json#/definitions/output"
    },
    "input": {
      "$ref": "definitions.json#/definitions/input"
    },
    "function": {
      "const": "skip_events()"
    },
    "hint": {
      "const": "Lets the pipeline skips the provided number of events."
    }
  },
  "required": [
    "id",
    "parameters",
    "outputs"
  ]
}